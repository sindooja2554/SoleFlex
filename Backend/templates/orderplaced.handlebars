<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Your Order Confirmation</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: auto;
      border: 1px solid #e2e2e2;
      border-radius: 10px;
      padding: 20px;
    }
    h2 {
      color: #4CAF50;
    }
    .summary {
      margin-top: 15px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f9f9f9;
    }
    .footer {
      margin-top: 25px;
      font-size: 0.9em;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>🎉 Thank you for your order!</h2>
    <p>Your order <strong>#{{data.orderId}}</strong> has been placed successfully on {{data.orderDate}}.</p>

    <div class="summary">
      <h3>🧾 Order Summary:</h3>
      <table>
        <thead>
          <tr>
            <th>Item</th>
            <th>Qty</th>
            <th>Price</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>{{data.name}}</td>
            <td>{{data.quantity}}</td>
            <td>${{data.price}}</td>
          </tr>
        </tbody>
      </table>
      <p><strong>Total:</strong> ${{data.totalAmount}}</p>
    </div>

    <div class="footer">
      <p>If you have any questions, feel free to contact our support team.</p>
      <p>Thanks again for shopping with us!</p>
    </div>
  </div>
</body>
</html>
